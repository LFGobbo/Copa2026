name: Cron Bolão

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Chama cron do Worker
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            -H "X-Cron-Secret: ${{ secrets.CRON_SECRET }}" \
            "https://copa2026-bolao.luizfelipegobbo.workers.dev/cron?task=all"
